import { DividerPipe } from '@shared/pipes/divider/divider.pipe';

describe('DividerPipe', () => {
  it('create an instance', () => {
    const pipe = new DividerPipe();
    expect(pipe).toBeTruthy();
  });
});
